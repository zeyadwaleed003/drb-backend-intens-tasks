export enum VehicleType {
  CAR = 'car',
  BUS = 'bus',
  TRUCK = 'truck',
  VAN = 'van',
  MOTORCYCLE = 'motorcycle',
}

export enum VehicleStatus {
  ASSIGNED = 'assigned',
  UNASSIGNED = 'unassigned',
}
